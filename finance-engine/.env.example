# FIN-L4 配置示例 — 复制为 .env 并修改
# 优先级: 环境变量 > .env > 内置默认值

# 服务监听
FIN4_HOST=127.0.0.1
FIN4_PORT=8500

# 数据目录（SQLite 存放位置）
FIN4_DB_DIR=~/.fin-l4

# 默认家庭 ID
FIN4_FAMILY_ID=default

# 调试模式 (0/1)
FIN4_DEBUG=0

# 外部系统链接只读模式 (1=只读跳转, 不自动同步)
FIN4_EXTERNAL_READONLY=1
