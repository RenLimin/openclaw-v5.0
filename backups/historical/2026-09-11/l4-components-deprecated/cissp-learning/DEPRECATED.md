# ⚠️ 已弃用 / Deprecated

**本组件已被 `cissp-trainer` 取代。**

迁移到 `cissp-trainer` 的原因：
- 完整的学习路径规划引擎（8 领域自适应）
- 模拟考试引擎（随机组卷 + 计时 + 评分 + 错题本）
- 知识图谱（领域关系 + 知识点依赖）
- 间隔重复算法 (SM-2)
- 110 个测试覆盖
- SQLite 结构化存储，扩展性更好

**本目录仅保留供历史参考，不再维护。**

## 最后状态

- 版本：v0.1 (MVP)
- 功能：每日学习计划 + 闪卡 + 题库（JSON 存储）
- 测试：0 个
- 迁移方式：可用 `cissp-trainer` 的 importer 导入旧题库

## 目录说明

| 文件/目录 | 说明 |
|-----------|------|
| `src/cissp/` | 旧版核心代码 |
| `data/` | JSON 格式的旧数据 |
| `docs/` | 旧版文档 |
| `SKILL.md` | 旧版 Skill（未迁移） |
| `DESIGN.md` | 旧版设计文档 |
