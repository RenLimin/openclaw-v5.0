[project]
name = "agent-slides"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Limin Ren", email = "bangcle@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
agent-slides = "agent_slides:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
